# Change history

- The initial plan `implementation_plan.md` was used as the authoritative plan at the time of implementation. 
- v2 clarifies that ML Kit OCR is `Task`-based and not pollable; `getResult()` before completion is invalid. This was discovered during debugging.

